{
  "name": "save-and-compress",
  "version": "1.0.0",
  "description": "A Figma plugin to compress images using TinyPNG API",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/ui.html dist/ui.html",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.64.0",
    "typescript": "^5.0.0"
  }
}
